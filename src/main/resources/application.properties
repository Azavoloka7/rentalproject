# DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3308/rentalproject?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Z@v010ka
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# ... other Hibernate properties as needed

# H2 Console Configuration (if needed)
# Commented out as it's not used
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.datasource.h2.url=jdbc:h2:mem:testdb
# spring.datasource.h2.driver-class-name=org.h2.Driver
# spring.datasource.h2.username=root
# spring.datasource.h2.password=Z@v010ka

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.zavoloka.rental=DEBUG

# Main Configuration
spring.application.admin.enabled=false
spring.main.allow-bean-definition-overriding=true
